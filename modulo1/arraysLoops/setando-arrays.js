const nomeDeEstudantes = ['jose', 'joao', 'maria', 'joana', 'andre'];

// trocar item de um array
// não está modificando a variável, e sim o conteúdo do array

const x = 3;

nomeDeEstudantes[0] = "andreia";
nomeDeEstudantes[x] = "epaminondas"
console.log(nomeDeEstudantes);